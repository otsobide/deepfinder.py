name: Tests

on:
    push:
    pull_request:
    schedule:
        - cron: '00 09 * * *'

permissions:
    contents: read

concurrency:
    group: tests-${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
    quality:
        name: Lint and type check
        runs-on: ubuntu-latest
        steps:
            - name: Clone and checkout branch
              uses: actions/checkout@v4

            - name: Set up Python
              uses: actions/setup-python@v5
              with:
                  python-version: '3.12'

            - name: Install project
              run: make install

            - name: Lint
              run: make lint

            - name: Type check
              run: make typecheck

    tests:
        name: Python ${{ matrix.python-version }}
        needs: quality
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                python-version: ['3.9', '3.10', '3.11', '3.12', '3.13', '3.14']
                os: ['ubuntu-latest']
                include:
                    - python-version: 'pypy3.11'
                      os: 'ubuntu-latest'
                    - python-version: '3.12'
                      os: 'macos-latest'
                    - python-version: '3.12'
                      os: 'windows-latest'
        steps:
            - name: Clone and checkout branch
              uses: actions/checkout@v4

            - name: Set up Python ${{ matrix.python-version }} in ${{ matrix.os }} OS.
              uses: actions/setup-python@v5
              with:
                  python-version: ${{ matrix.python-version }}

            - name: Install project
              run: make install

            # Coverage is measured on CPython only. PyPy's tracing is not reliable
            # enough for it -- the doctest suite installs its own trace function and
            # coverage reports "Trace function changed, data is likely wrong", which
            # trips the 100% gate on a measurement artefact rather than a real gap.
            - name: Run tests with coverage
              if: ${{ !startsWith(matrix.python-version, 'pypy') }}
              run: make coverage

            - name: Run tests
              if: ${{ startsWith(matrix.python-version, 'pypy') }}
              run: make test
